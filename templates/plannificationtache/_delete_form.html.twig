<form method="post" action="{{ path('app_plannificationtache_delete', {'id': plannificationtache.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plannificationtache.id) }}">
    <button class="btn">Delete</button>
</form>
