<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tâche Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body.modal-open {
            overflow: hidden;
            position: relative;
        }
        .modal-backdrop {
            backdrop-filter: blur(3px);
            background-color: rgba(0,0,0,0.2);
        }
        .task-modal {
            max-width: 800px;
            box-shadow: 0 0 20px rgba(0,0,0,0.3);
        }
        .task-details-table th {
            width: 30%;
        }
        .background-content {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            opacity: 0.5;
            background-color: #f8f9fa;
            background-image: url('data:image/svg+xml;base64,');
        }
        .modal-content {
            border: none;
            border-radius: 10px;
        }
    </style>
</head>
<body class="bg-light">
    {% block body %}
    <!-- Background content (visible through blur) -->
    <div class="background-content"></div>

    <!-- Main content with blur effect when modal is open -->
    <div class="container py-5" id="main-content">
        <div class="text-center mb-4">
            <h2 class="text-muted">Application de Gestion des Tâches</h2>
            <p class="text-muted">Détails de la tâche en cours de visualisation</p>
        </div>
        
        <!-- Simulated previous page content -->
        <div class="card shadow-sm mb-3" style="opacity: 0.5;">
            <div class="card-body">
                <h5 class="card-title">Liste des Tâches</h5>
                <div class="list-group">
                    <a href="#" class="list-group-item list-group-item-action active">
                        Tâche #{{ tache.id }} (actuelle)
                    </a>
                    {% for i in 1..3 %}
                    <a href="#" class="list-group-item list-group-item-action">
                        Tâche #{{ tache.id + i }}
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

    <!-- Task Details Modal -->
    <div class="modal fade show d-block" tabindex="-1" aria-modal="true" role="dialog" style="display: block !important;">
        <div class="modal-dialog modal-lg task-modal">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title">
                        <i class="bi bi-card-checklist"></i> Détails de la Tâche #{{ tache.id }}
                    </h5>
                    <button type="button" class="btn-close btn-close-white" onclick="window.history.back();"></button>
                </div>
                <div class="modal-body">
                    <table class="table task-details-table">
                        <tbody>
                            <tr class="align-middle">
                                <th>ID</th>
                                <td>{{ tache.id }}</td>
                            </tr>
                            <tr class="align-middle">
                                <th>ID Centre</th>
                                <td>{{ tache.idCentre }}</td>
                            </tr>
                            <tr class="align-middle">
                                <th>ID Employé</th>
                                <td>{{ tache.idEmploye }}</td>
                            </tr>
                            <tr class="align-middle">
                                <th>Localisation</th>
                                <td>
                                    <div><small class="text-muted">Altitude:</small> {{ tache.altitude }}</div>
                                    <div><small class="text-muted">Longitude:</small> {{ tache.longitude }}</div>
                                </td>
                            </tr>
                            <tr class="align-middle">
                                <th>Message</th>
                                <td>
                                    <div class="p-3 bg-light rounded">
                                        {{ tache.message }}
                                    </div>
                                </td>
                            </tr>
                            <tr class="align-middle">
                                <th>État</th>
                                <td>
                                    {% if tache.etat == 'Terminé' %}
                                        <span class="badge bg-success rounded-pill p-2">{{ tache.etat }}</span>
                                    {% elseif tache.etat == 'En cours' %}
                                        <span class="badge bg-warning rounded-pill p-2">{{ tache.etat }}</span>
                                    {% elseif tache.etat == 'En attente' %}
                                        <span class="badge bg-secondary rounded-pill p-2">{{ tache.etat }}</span>
                                    {% else %}
                                        <span class="badge bg-primary rounded-pill p-2">{{ tache.etat }}</span>
                                    {% endif %}
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
                <div class="modal-footer">
                    <a href="{{ path('app_tache_index') }}" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Retour à la liste
                    </a>
                    <div class="btn-group">
                        <a href="{{ path('app_tache_edit', {'id': tache.id}) }}" class="btn btn-primary">
                            <i class="bi bi-pencil"></i> Modifier
                        </a>
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
                            <i class="bi bi-trash"></i> Supprimer
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-backdrop fade show"></div>

    <!-- Delete Confirmation Modal -->
    <div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-danger text-white">
                    <h5 class="modal-title">Confirmer la suppression</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>Êtes-vous sûr de vouloir supprimer cette tâche ? Cette action est irréversible.</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                    <form method="post" action="{{ path('app_tache_delete', {'id': tache.id}) }}" class="d-inline">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tache.id) }}">
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash"></i> Confirmer la suppression
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Force the modal to show on page load
        document.addEventListener('DOMContentLoaded', function() {
            document.body.classList.add('modal-open');
            
            // Handle back button to close the modal
            document.querySelector('.modal-header .btn-close').addEventListener('click', function() {
                window.history.back();
            });
        });
    </script>
    {% endblock %}
</body>
</html>