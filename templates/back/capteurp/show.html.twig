<!DOCTYPE html>

<title>Capteurp</title>

{% block body %}
    <h1>Capteurp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ capteurp.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ capteurp.quantite }}</td>
            </tr>
            <tr>
                <th>Date_m</th>
                <td>{{ capteurp.dateM ? capteurp.dateM|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_capteurp_index') }}">back to list</a>

    <a href="{{ path('app_capteurp_edit', {'id': capteurp.id}) }}">edit</a>

    {{ include('back/capteurp/_delete_form.html.twig') }}
{% endblock %}
