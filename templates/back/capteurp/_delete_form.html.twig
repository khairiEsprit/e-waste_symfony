<form method="post" action="{{ path('app_capteurp_delete', {'id': capteurp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ capteurp.id) }}">
    <button class="btn">Delete</button>
</form>
