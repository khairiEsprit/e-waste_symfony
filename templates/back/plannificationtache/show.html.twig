<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails de la Tâche</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        .detail-card {
            max-width: 800px;
            margin: 0 auto;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .table-details {
            margin-bottom: 0;
        }
        .table-details tr:last-child td {
            border-bottom: none;
        }
        .action-buttons {
            margin-top: 20px;
        }
        .card-header {
            border-radius: 10px 10px 0 0 !important;
        }
    </style>
</head>
<body>
    {% block body %}
    <div class="container py-5">
        <div class="card detail-card">
            <div class="card-header bg-primary text-white">
                <h1 class="h3 mb-0">Détails de la Tâche</h1>
            </div>
            
            <div class="card-body">
                <table class="table table-details">
                    <tbody>
                        <tr>
                            <th class="w-25">ID</th>
                            <td>{{ plannificationtache.id }}</td>
                        </tr>
                        <tr>
                            <th>Priorité</th>
                            <td>
                                <span class="badge 
                                    {% if plannificationtache.priorite == 'Haute' %}bg-danger
                                    {% elseif plannificationtache.priorite == 'Moyenne' %}bg-warning text-dark
                                    {% else %}bg-success
                                    {% endif %}">
                                    {{ plannificationtache.priorite }}
                                </span>
                            </td>
                        </tr>
                        <tr>
                            <th>Date limite</th>
                            <td>
                                <span class="{% if date(plannificationtache.dateLimite) < date() %}text-danger{% endif %}">
                                    {{ plannificationtache.dateLimite ? plannificationtache.dateLimite|date('d/m/Y') : 'Non définie' }}
                                    {% if date(plannificationtache.dateLimite) < date() %}
                                        <i class="bi bi-exclamation-triangle-fill text-danger ms-2" title="Date dépassée"></i>
                                    {% endif %}
                                </span>
                            </td>
                        </tr>
                    </tbody>
                </table>

                <div class="d-flex justify-content-between action-buttons">
                    <a href="{{ path('app_plannificationtache_index') }}" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Retour à la liste
                    </a>
                    
                    <div>
                        <a href="{{ path('app_plannificationtache_edit', {'id': plannificationtache.id}) }}" class="btn btn-primary me-2">
                            <i class="bi bi-pencil"></i> Modifier
                        </a>
                        {{ include('back/plannificationtache/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% endblock %}

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>