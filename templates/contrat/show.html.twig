{% extends 'back/base.html.twig' %}

{% block title %}Détails du Centre{% endblock %}

{% block content %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>Id_centre</th>
                <td>{{ contrat.idCentre }}</td>
            </tr>
            <tr>
                <th>Id_employe</th>
                <td>{{ contrat.idEmploye }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ contrat.dateDebut ? contrat.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ contrat.dateFin ? contrat.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>SignaturePath</th>
                <td>{{ contrat.signaturePath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contrat_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_contrat_edit', {'id': contrat.id}) }}"class="btn btn-primary">edit</a>

    {{ include('contrat/_delete_form.html.twig') }}
{% endblock %}
